let number = 52; //numerico,  entero
let decimal = 52.2; //numerico, decimal
let string = 'hola';// cadena de caracteres

let edad = "25"
let EdadNumber = Number(edad);

let esMayorDeEdad = true; booleano

//los objetos son un tipo de dato que pueden alojar otro tipo de datos
let persona = {     //LOS OBJETOS ALOJAN PROPIEDADES
    nombre: "Javier",
    edad: 25,
    vive: true
}

// Array
let array = ["string", persona, {name: "Matias"}, true, 35]

//undefined

let indefinido;

//Not a Number(NaN)
let numero = Number("1S")

let nulo = null;
